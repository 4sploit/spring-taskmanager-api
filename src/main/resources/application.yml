spring:
  profiles:
    active: "dev,mysql,locale,okta,actuator"
  application:
    name: ${APP_NAME:taskmanager}
  data:
    rest:
      basePath: ${API_BASE_PATH:/api}
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  # jackson:
  #   default-property-inclusion: non-null
